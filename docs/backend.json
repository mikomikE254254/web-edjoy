{
  "entities": {
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product in the Runway Retail application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the product."
        },
        "slug": {
          "type": "string",
          "description": "URL-friendly slug for the product."
        },
        "name": {
          "type": "string",
          "description": "The name of the product."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the product."
        },
        "price": {
          "type": "number",
          "description": "The price of the product."
        },
        "originalPrice": {
          "type": "number",
          "description": "The original price before a discount."
        },
        "category": {
          "type": "string",
          "description": "The category of the product.",
          "enum": ["men", "women", "bags", "children"]
        },
        "style": {
          "type": "string",
          "description": "The style of the product.",
          "enum": ["casual", "streetwear", "formal", "vintage", "minimal"]
        },
        "images": {
          "type": "array",
          "description": "Product images. The first image is the main image.",
          "items": {
            "type": "object",
            "properties": {
              "url": { "type": "string", "format": "uri" },
              "alt": { "type": "string" },
              "hint": { "type": "string" },
              "colorName": { "type": "string" }
            },
            "required": ["url", "alt"]
          }
        },
        "availableColors": {
          "type": "array",
          "description": "Available product colors.",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "hex": { "type": "string" }
            },
            "required": ["name", "hex"]
          }
        },
        "sizes": {
          "type": "array",
          "description": "Available product sizes.",
          "items": { "type": "string" }
        }
      },
      "required": ["id", "slug", "name", "price", "category", "images"]
    },
    "Category": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Category",
      "type": "object",
      "description": "Represents a category of products (e.g., Casual, Streetwear).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the category entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the category."
        },
        "description": {
          "type": "string",
          "description": "A description of the category."
        },
        "productIds": {
          "type": "array",
          "description": "References to Products. (Relationship: Category 1:N Product)",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "name",
        "description"
      ]
    },
    "Review": {
      "title": "Review",
      "type": "object",
      "description": "Represents a user's rating for a product.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user who left the review."
        },
        "rating": {
          "type": "number",
          "description": "The star rating from 1 to 5."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the review was last updated."
        }
      },
      "required": ["userId", "rating"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Stores product information.  The `productId` is the document ID.",
          "params": [
            {
              "name": "productId",
              "description": "The unique identifier for the product."
            }
          ]
        }
      },
      {
        "path": "/products/{productId}/reviews/{userId}",
        "definition": {
          "entityName": "Review",
          "schema": {
            "$ref": "#/backend/entities/Review"
          },
          "description": "Stores user ratings for a product. The `userId` is the document ID and corresponds to the user's UID.",
          "params": [
            { "name": "productId", "description": "The ID of the product being reviewed." },
            { "name": "userId", "description": "The UID of the user leaving the review." }
          ]
        }
      },
      {
        "path": "/categories/{categoryId}",
        "definition": {
          "entityName": "Category",
          "schema": {
            "$ref": "#/backend/entities/Category"
          },
          "description": "Stores category information.  The `categoryId` is the document ID.",
          "params": [
            {
              "name": "categoryId",
              "description": "The unique identifier for the category."
            }
          ]
        }
      }
    ],
    "reasoning": "The data structure prioritizes authorization independence and QAPs (Rules are not filters). Products are stored in a top-level `products` collection. Categories are also stored in a top-level `categories` collection. The `Category` document contains an array of product IDs. This structure avoids hierarchical `get()` calls in security rules.  The categories document `productIds` enables QAPs (listing products is secure, even when those categories have a large number of products)."
  }
}
